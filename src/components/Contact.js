import React from 'react';

const Contact = (props) => {
    // setTimeout(() => {
    //     props.history.push('/about')
    // }, 2000); CMD+K,C
    return(
        <div className="container">
            <h4 className="centers">Contact</h4>
            <p>This is a random paragraph!</p>
        </div>
    )
}

export default Contact;